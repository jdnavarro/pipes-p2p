- Fix ADDR/GETADDR.
- Remove closed Unix socket files when exiting.
- Handle `Decoding` errors: it should ignore input and resume listening.
- Extract `Message` implementation in different package.
- Command line interface and configurator (different package?).
